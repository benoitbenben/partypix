<div class="background_rainbow">

  <div class="container">

    <div class="row">

      <div class="year">2018</div>

        <% @rooms.each do |room| %>

          <div class="col-xs-12">

            <div class="flat-card">

              <div class="flat-card description">
                <div class= "title"><%= room.name %></div>
                <div class= "date"><%= room.date %></div>
                <div class="flat-card picture">
                <%= image_tag("PartyPix_main_logo.png", size: "100x100") %>
                </div>
              </div>



              <%= link_to "", room_path(room), class: "flat-card-link" %>
            </div>

          </div>

        <% end %>

    </div>

  </div>

</div>
